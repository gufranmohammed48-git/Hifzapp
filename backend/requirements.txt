# Backend dependencies for the Hifzapp ASR (Zipformer Quran model).
#
# sherpa-onnx is the official k2-fsa runtime for icefall Zipformer
# streaming CTC models. It bundles onnxruntime, numpy, and handles
# all the streaming state management, mel feature extraction, and
# CTC decoding internally — so we don't have to.
#
# Note: this image is MUCH smaller than the previous NeMo-based one
# (~50MB vs ~2GB).

# Streaming ASR runtime (includes onnxruntime + numpy internally)
sherpa-onnx>=1.10.0,<2.0.0

# Web server
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0

# Used by the build-time downloader (optional, only needed if
# SKIP_DOWNLOAD=0 is unset in docker-compose.yml)
huggingface_hub==0.23.0
